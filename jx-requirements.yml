autoUpdate:
  enabled: true

cluster:
  clusterName: jx
  environmentGitOwner: salmmalop
  gitKind: github
  gitName: github
  gitServer: https://github.com

environments:
  - key: dev
  - key: staging
  - key: production

repository: nexus
secretStorage: local

storage:
  logs:
    enabled: true
  reports:
    enabled: true
  repository:
    enabled: true

vault:
  name: vault
  bucket: jx-vault
  key: vault-key
